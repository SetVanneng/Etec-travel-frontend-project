// PostCSS runs Tailwind's CSS. Tailwind reads your source files,
// finds the classes you use, and generates only those styles.
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}